{{#each blogs}}

<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2> {{user.name}}</h2>
    <span>Date: {{date_created}}</span>
  </div>
  <h2 data-id="{{id}}"> {{title}} </h2>
  <p>{{content}}</p>
</div>
{{#each comments as |comment|}}
<div>
  <h4>{{user.name}} <span>{{date_created}}</span></h4>
  <p>{{content}}</p>
</div>
{{/each}}
{{/each}}